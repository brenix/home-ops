---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mongodb
spec:
  chartRef:
    kind: OCIRepository
    name: mongodb
  interval: 1h
  values:
    controllers:
      mongodb:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/library/mongo
              tag: 8.3.4@sha256:0f887198e29c093fd2b36c3e2eb43c7b98e47c081d89fbd5bc212da0cd43ec58
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 1Gi
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
        type: statefulset
    service:
      app:
        ports:
          mongodb:
            enabled: true
            port: 27017
            primary: true
            protocol: TCP
        type: ClusterIP
    persistence:
      data:
        enabled: true
        existingClaim: "mongo-data"
        globalMounts:
        - path: /data/db
      initdb:
        enabled: true
        globalMounts:
        - path: /docker-entrypoint-initdb.d
        name: mongodb-init
        type: secret
